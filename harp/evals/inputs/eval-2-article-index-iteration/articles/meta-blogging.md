# <%= title %>

*<%= date %>*

I've been slowly walking back from social media. Not in a dramatic way — I haven't deleted any accounts and I still scroll occasionally — but the active posting has stopped. The thing that replaced it, gradually, is this: writing in a folder of markdown files and pushing it to a static host.

The posts are longer. The cadence is slower. Almost no one will read them. All of those feel like features.
